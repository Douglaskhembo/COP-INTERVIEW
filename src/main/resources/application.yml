spring:
  application:
    name: PaymentsApp

  datasource:
    url: jdbc:postgresql://localhost:5432/cop_db
    username: admin
    password: pass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
server:
  port: 8088
